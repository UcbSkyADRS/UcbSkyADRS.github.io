{
	"name": "ucb-sky-adrs",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"dev": "next dev",
		"build": "next build",
		"start": "next start",
		"export": "next build && next export && touch ./out/.nojekyll && rm -rf ./docs && mv ./out ./docs && next-sitemap",
		"deploy": "npm run export"
	},
	"dependencies": {
		"next": "^12.3.4",
		"react": "18.1.0",
		"react-dom": "18.1.0",
		"react-icons": "^4.12.0"
	},
	"devDependencies": {
		"autoprefixer": "^10.4.7",
		"next-sitemap": "^4.2.3",
		"postcss": "^8.4.14",
		"tailwindcss": "^3.1.2"
	}
}
